# GameController-xinput2esp32
Use gamepad connected to PC to control an ESP32 device over UDP (e.g. PC "driver station" controlling an ESP-32 robot via wifi)

Coming soon:
1. Example Arduino sketch to use this library
2. NetInput Capture program to send PC gamepad events via UDP to the Arduino; derived from https://github.com/usertoroot/NetInput
